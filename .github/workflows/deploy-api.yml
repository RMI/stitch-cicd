on:
  workflow_call:
    inputs:
      full-image-name:
        description: "Full pushed image name including host/registry, name, and tag"
        required: true
        type: string
      containerAppName:
        description: "The name of the Azure Container App."
        required: true
        type: string
      containerAppEnvironment:
        description: "The name of the Azure Container App environment to deploy to."
        required: false
        default: "stitch-dev"
        type: string
      targetPort:
        description: "Port the applications listens to"
        required: false
        default: 8000
        type: number
    outputs:
      container-app-url:
        description: "HTTPS URL of the deployed Azure Container App"
        value: ${{ jobs.deploy-api.outputs.container-app-url }}
      container-app-fqdn:
        description: "Ingress FQDN of the deployed Azure Container App"
        value: ${{ jobs.deploy-api.outputs.container-app-fqdn }}

jobs:
  deploy-api:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: read
    timeout-minutes: 25
    outputs:
      container-app-url: ${{ steps.export-app-url.outputs.container_app_url }}
      container-app-fqdn: ${{ steps.export-app-url.outputs.container_app_fqdn }}

    steps:

      - name: Azure login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - run: echo "fullname=${{ inputs.full-image-name }}"

      - name: Build and deploy Container App
        uses: azure/container-apps-deploy-action@v1
        with:
          imageToDeploy: ${{ inputs.full-image-name }}
          resourceGroup: STITCH-DEV-RG
          containerAppName: ${{ inputs.containerAppName }}
          containerAppEnvironment: ${{ inputs.containerAppEnvironment }}
          targetPort: ${{ inputs.targetPort }}
          environmentVariables: LOG_LEVEL=debug AUTH_DISABLED=true ENVIRONMENT=${{ (github.event_name == 'pull_request' && format('{0}-{1}', 'pr', github.event.pull_request.number)) || github.ref_name }}
          ingress: "external"

      - name: Export application URL
        id: export-app-url
        shell: bash
        run: |
          set -euo pipefail

          fqdn="$(
            az containerapp show \
              --name "${{ inputs.containerAppName }}" \
              --resource-group "STITCH-DEV-RG" \
              --query "properties.configuration.ingress.fqdn" \
              --output tsv
          )"

          if [ -z "$fqdn" ]; then
            echo "Failed to resolve container app ingress FQDN" >&2
            exit 1
          fi

          echo "Resolved FQDN: $fqdn"
          echo "container_app_fqdn=$fqdn" >> "$GITHUB_OUTPUT"
          echo "container_app_url=https://$fqdn" >> "$GITHUB_OUTPUT"

  add_comment:
    if: ${{ github.event.pull_request.number != null }}
    needs: [deploy-api]
    runs-on: ubuntu-latest
    steps:
      - name: Create or update comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            API Deploy Status ${{ github.head_ref }}
            url='${{ needs.deploy-api.outputs.container-app-url }}'
            fqdn='${{ needs.deploy-api.outputs.container-app-fqdn }}'
          edit-mode: replace
